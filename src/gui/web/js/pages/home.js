/**
 * home.js — Homepage placeholder (content will be redesigned)
 */
(function() {

    function render(container) {
        container.innerHTML = '';
    }

    App.registerPage('home', { render });
})();
