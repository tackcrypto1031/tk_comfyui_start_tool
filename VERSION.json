{
  "version": "0.5.2",
  "min_python": "3.10",
  "release_notes": "修復舊版本(含 0.2.9)升級因 config.json 衝突失敗的問題",
  "changes": [
    "修復自動更新:舊版本升級時 config.json 會被自動備份並還原,不再因「local changes would be overwritten by merge」而中止",
    "config.json 改為使用者私有檔,不再納入 git 追蹤;新增 config.default.json 作為新安裝的種子模板",
    "fs_ops.load_config() 優先從 config.default.json 載入預設值,repo 更新的預設項(如擴充的 model_subdirs)會自動補進既有設定"
  ],
  "changes_i18n": {
    "en": [
      "Fixed auto-updater: config.json is now backed up and restored around git pull, so upgrades from older versions (including 0.2.9) no longer abort with 'local changes would be overwritten by merge'",
      "config.json is now user-private and no longer tracked in git; a new config.default.json ships as the seed template for fresh installs",
      "fs_ops.load_config() now pulls defaults from config.default.json first, so repo-tuned defaults (e.g. expanded model_subdirs) flow into existing user configs automatically"
    ],
    "zh-TW": [
      "修復自動更新:舊版本升級時 config.json 會被自動備份並還原,不再因「local changes would be overwritten by merge」而中止",
      "config.json 改為使用者私有檔,不再納入 git 追蹤;新增 config.default.json 作為新安裝的種子模板",
      "fs_ops.load_config() 優先從 config.default.json 載入預設值,repo 更新的預設項(如擴充的 model_subdirs)會自動補進既有設定"
    ]
  }
}
